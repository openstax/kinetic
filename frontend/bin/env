#!/usr/bin/env bash

set -eo pipefail

# these also must be set as an ARG in Dockerfile
export VITE_SENTRY_DSN=$SENTRY_DSN
export VITE_GTAG_ID=$GTAG_ID

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

$(npm bin)/$1 "${@:2}"
